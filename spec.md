## 圍棋排譜工具開發規格書 (Spec Task List)
1. 視覺與 UI 元件 (Super Realistic Visuals)
目標是打造具有溫度的數位棋具，減少塑膠感。

[Task 1.1] 棋盤材質與渲染 (Canvas)：

選用高解析度的木紋貼圖（推薦榧木紋理）作為背景。

使用 CSS box-shadow 為 Canvas 容器邊緣增加立體厚度感。

使用 Canvas API 繪製網格線（19x19），使用 0.5px 的黑色實線，透明度設為 0.7，模擬墨水滲入木頭的視覺。

[Task 1.2] 棋子元件實作：

黑子： 使用 radial-gradient (深灰到純黑)，並加上微量的 drop-shadow。

白子： 使用 radial-gradient (純白到淡奶油色)，增加珍珠光澤感。

落子動畫： 加上極短的縮放效果（Scale）與淡入，模擬棋子接觸棋盤的瞬間。

[Task 1.3] 介面佈局：

側邊或下方提供「控制面板」：後退、快進、清空棋盤。

2. 核心邏輯演算法 (Go Logic Engine)
這部分是程式的靈魂，負責處理圍棋繁瑣的吃子與打劫規則。

[Task 2.1] 氣的運算 (Liberties Calculation)：

實作 BFS (廣度優先搜尋) 或 DFS (深度優先搜尋) 演算法。

當一顆棋子落子後，檢查四周相連棋塊的「氣」。

若某棋塊氣數為 0，觸發自動提子邏輯。

[Task 2.2] 棋盤狀態儲存：

使用 Immutable.js 或簡單的 Array 快照來記錄棋盤。

每一步落子必須記錄：(x, y, color, [capturedStones])。

[Task 2.3] 打劫 (Ko) 與禁著點提示：

判斷打劫與沒有氣且無法提掉對方棋子的位置。

考量自由覆盤需求，不強制禁止落子，而是給予「紅色警示」的視覺提示。

3. 排譜功能 (History & Playback)
針對「排譜」需求，確保紀錄的精確與可追溯。

[Task 3.1] 歷史堆疊 (History Stack)：

建立 past 與 future 兩個陣列實作 Undo/Redo。

後退 (Undo)： 撤銷落子，並將該步被吃掉的棋子（capturedStones）重新渲染回畫面上。

[Task 3.2] 導航控制：

支援手指滑動（Swipe）或點擊按鈕來快速切換手數。

顯示目前總手數與黑白提子數統計。

4. iPad 觸控優化 (iPad UX)
[Task 4.1] 防止手掌誤觸： 使用 CSS touch-action: none 鎖定縮放與原生手勢。

[Task 4.2] 落子輔助： 實作長按後的「放大鏡」或是「預落子點」顯示，避免手指太粗點錯格子。

[Task 4.3] 音效： 接入高品質的木頭撞擊音效（需確保由第一次使用者點擊事件解鎖 AudioContext）。

## 開發階段建議 (Step-by-Step)
第一階段： 跑通 React + Canvas 的基本棋盤繪製，先讓單機「黑白交替落子」動起來。

第二階段： 寫好「吃子演算法」與「紅色警示」邏輯，這是最花腦袋的部分，需要反覆測試。

第三階段： 實作 Undo/Redo，確保吃掉的子可以正確「復活」。

第四階段： 實作 iPad 觸控優化（防誤觸、放大鏡）。

第五階段： 視覺微調，把木紋、棋子光澤、音效加進去。